export function isAuthenticated() {
  // Frontend-only demo: Authentication is bypassed for demonstration purposes
  // In a production environment with backend, this would check for valid auth tokens
  return true;
}
